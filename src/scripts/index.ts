import * as Phaser from "phaser";

new Phaser.Game({
    type: Phaser.AUTO,
    parent: "sapper",
    width: window.innerWidth,
    height: window.innerHeight,
    backgroundColor: "#f0ffff",
    scene: []
});
